// While condition will work as like loop

var number = 25;

while (number > 20 ){
    console.log('Hello world!');
    number -= 2;
    // number ++;
    // number = number+ 5;
    // console.log(number);
}
